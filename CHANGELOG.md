# 更新日志

## Unreleased

- **双击 exe 直接进入 gui 模式**：无参数启动等价 `gui`（托盘常驻 + 浏览器自动
  打开控制面板），双击启动时自动隐藏随进程创建的控制台窗口（终端里执行不隐藏，
  日志照常可见）；gui 启动失败（如已有实例在运行）时弹出消息框提示，不再静默退出
- **应用图标重新设计**：渐变圆角底 + 白色剪贴板 + 双向同步箭头，覆盖
  16~256 全尺寸；exe 打包嵌入同一图标（`rsrc_windows_amd64.syso`，
  含版本信息资源），系统托盘与面板 favicon 同步使用（`tools/icon/gen_icon.ps1`）

- **破坏性变更：废弃共享 token 认证**。config.json 不再有 `token` 字段，
  鉴权只认配对令牌——未配对的节点无法传输、无法 KVM 跨屏。
  （此前"各节点 token 改成一致"的互通用法随之失效，请升级后在面板重新配对）
- **KVM 布局跨机自动同步**：面板「屏幕布局」配置左右邻后，布局经配对通道推送
  到对端（自动互为镜像），本机与对端均即时热生效，无需重启、无需两端重复配置；
  新增传输类型 `kvm` 承载该同步
- 新增**配对认证**：gui 面板「在线节点」点击「配对」→ 对端面板确认
  「接受配对」即完成；双方交换逐对独立的配对令牌，存于 `~/.copywhere/peers.json`
  （新增，支持解除配对/吊销）（`internal/trust`）
- 传输协议新增 `pair` 类型（人工确认的凭据交换，应答沿原连接返回不可伪造）；
  协议头新增 `sender_id` 字段用于配对令牌校验
- 面板新增「屏幕布局」视图：仿 Windows 多显示器设置，设备渲染为显示器卡片，
  拖动到本机左右空位即完成 KVM 左邻/右邻配置（支持点选快捷按钮、交换位置、
  单侧清除、离线邻居灰色展示）；KVM 开关可在面板编辑（开关重启生效）
- 面板「在线节点」显示配对状态（已配对/未配对/已被拒）与配对按钮；
  收到配对请求弹出确认框；设置页新增「已配对设备」管理
- 新增 GUI 模式（`copywhere gui`）：系统托盘 + 浏览器控制面板，
  与 TUI（`run`）共存；面板以传输动态为主页（收发时间线 + 实时进度），
  支持全局拖拽投送、卡片式节点列表、终端风格日志抽屉、侧滑设置面板、
  深浅双主题（`internal/webui`）
- 托盘菜单：打开面板、暂停/恢复自动同步、打开接收目录、退出
- 手动发送支持面板拖入/选择文件上传（不受阈值限制）与文本发送，
  大文件传输实时进度条
- 面板设置页：节点名、自动阈值、接收目录、自动回贴、文本同步
  在线编辑（节点名重启生效，其余立即生效）
- 接收记录可从面板一键打开文件所在目录；新增 `web_port` 配置项
  （默认 47890，负数=随机端口），面板仅监听 127.0.0.1 + 随机访问令牌
- 剪贴板监控支持暂停开关（暂停期间内容不累积，恢复后不补发），
  阈值与文本同步开关修改后即时生效
- `gui` 以命名互斥锁防止与 `run` 双实例运行；面板地址落盘
  `~/.copywhere/panel_url.txt` 备查

## v0.2.0 (2026-09-09)

- 新增鼠标键盘跨屏（KVM，实验性）：边缘推动切换、输入转发与注入、
  看门狗自动释放、Ctrl+Alt+Shift+X 紧急退出（`internal/input`、`internal/kvm`）
- KVM 多显示器支持：暴露边缘检测（双屏内侧边界不误触发）、被控入口恒在
  主显示器共享边缘、相对位移注入（副机本地多屏自然跨越）、可配置入口屏
  （`kvm_entry_monitor`）
- KVM 入口点按主控光标比例映射到被控主屏同比例高度；绝对定位改用
  `SetCursorPos`（物理虚拟桌面像素），修复被控双屏时入口总跑到主屏角落
- DPI 感知升级 Per-Monitor v2（Win10 1703+，失败退回 System Aware），
  多屏不同缩放率下枚举与注入坐标系一致
- 修复 KVM 钩子线程死锁导致全系统鼠标卡顿（显示器缓存改独立锁 + 后台刷新）
- 修复 KVM accept 瞬时错误导致监听静默退出（表现为握手超时）
- 修复 KVM 空闲断连：被控端回应 ping（pong），主控端读超时不再误判
- 回退 Raw Input 设备句柄过滤（部分物理鼠标无句柄导致跨屏失效）
- 地址优选：192.168 局域网恒优先于虚拟网段（ZeroTier 10.*）

## v0.1.0 (2026-09-08)

首个版本。

- 节点自动发现：UDP 广播（各网段定向广播 + 回环），稳定机器指纹 + 会话标识
- 多网卡地址合并：同一节点全部地址保留，同网段直连优选，发送逐地址尝试
- 剪贴板同步：文件（CF_HDROP）与文本自动推送，接收端自动回贴剪贴板
- 三页终端界面：日志 / 在线节点 / 文件记录（Tab/←→ 切换，↑↓/PgUp/PgDn 滚动）
- TCP 传输：sha256 校验、临时文件原子改名、同名同内容去重
- 慢链路友好超时预算（256KB/s 估算），失败原因记录在对端日志
- token 鉴权；不一致提示一次即暂停发送，对端重启后自动恢复
- 多文件/目录自动打包 zip；`send` 命令手动发送不受阈值限制
- 防回环：剪贴板序号 + 接收冷却期双重防护
- 配置位于 `~/.copywhere/config.json`，接收目录 `~/.copywhere/files`
