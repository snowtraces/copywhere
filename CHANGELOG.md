# 更新日志

## v0.4.0 (2026-09-17)

- **新增**：**富文本剪贴板同步**——复制 Word/网页/邮件等带格式内容时，
  HTML Format 与 Rich Text Format 随纯文本一并搬运，对端 Ctrl+V 粘贴保留
  字体、颜色与排版（借鉴 MouseWithoutBorders 的 RTF 同步并扩展为三格式并存）。
  通过节点公告的 `caps:["rich"]` 做能力协商，对旧版本自动降级为纯文本通道，
  混合版本环境无需统一升级。单次富负载上限 16MB（超限自动降级纯文本）。
  两端写入前均做魔数轻校验（HTML 须 `Version:` 头、RTF 须 `{\rtf` 开头），
  畸形内容只保留纯文本；README「已知限制」注明富文本写回剪贴板的信任边界。
- **改进**：**剪贴板检测改为事件驱动**——用 `AddClipboardFormatListener` 注册
  消息专用窗口，复制动作毫秒级即触发同步（此前只有 400ms 轮询）。安装失败
  自动回退旧式 `SetClipboardViewer` 链，再不行保留轮询兜底，三条路径汇入同一
  检测循环；面板 `api/status` 新增 `watch_mode` 字段可查当前生效方式。
- **新增**：**运行时诊断**（`internal/diag` + 面板 `/api/debug`）——剪贴板事件、
  收发字节与连接、KVM 会话、发现公告等关键路径全部原子打点，连同环形日志、
  协程/堆内存与心跳年龄提供 JSON 快照与自动刷新的网页视图；announce /
  monitor / flush 主循环按各自预算由 watchdog 判定卡死并自动转储协程栈一次，
  用于定位"同步突然不动了"这类只有现场才有价值的故障。
- **新增（实验性）**：触控板双指滚动跨屏识别——针对 PTP 双指滚动属于
  `PT_GESTURE2` 指针手势、不经低级钩子/Raw Input 而无法跨屏的已知问题，
  绕过指针层直接旁路监听触控板 HID 原始触点（Digitizer Raw Input +
  preparsed data 解析），自研双指滚动状态机，在 KVM 主控接管期间转成滚轮
  事件转发给副机（默认关闭，面板「设置 → 跨屏控制」可开）。影子监听：
  本机窗口仍会滚动。滚动方向经真机校准；输出倍率可在面板调节
  （`kvm_touchpad_speed`，50%~400%，即时生效）。滚轮来源仲裁：钩子滚轮
  （系统合成的 legacy 滚轮）为权威源，双指手势期间出现即让识别器让位
  （起步 100ms 缓冲避免首格双发），识别器只在系统未合成滚轮的指针手势
  区域输出，外接鼠标滚轮不受影响。实现与限制详见
  `docs/touchpad-gesture-rawinput-plan.md`。
- **改进**：**KVM 跨分辨率与实际设置缩放比自适应补偿 + 亚像素余数结转**：
  引入 Windows 原生 `GetDpiForMonitor` 读取两端各显示器的实际 DPI 与设置缩放比（如 ThinkBook 2880×1800 @ 200%）。
  握手阶段交换双方物理尺寸与缩放比，通过逻辑视口换算自适应基准增益（$gain = \frac{W_s \times Scale_m}{W_m}$），
  彻底解决高分缩放屏导致被控端光标极度迟缓的缺陷，会话期倍率恒定，**0 漂移、绝不再忽快忽慢**；
  引入亚像素余数结转（Sub-pixel Carryover），消除微步截断台阶感；
  多屏被控端自然协商相对通道，光标可在被控端各副屏间自由漫游。
  面板恢复「速度微调倍率」滑块（默认 100% 科学基准），支持在 50%~300% 范围内实时热调手感。
- **改进**：**KVM 鼠标移入/移出流畅度全面优化**：
  针对边缘切换偶发卡顿、需反复蹭边的痛点，彻底移除 1.5 秒硬性冻结，改为 200ms 防抖短冷却（失败时才锁 1s 防日志刷屏）；
  贴边感应区从 2px 拓宽至 6px，适配高分屏快速划动与斜推；
  推边阻力阈值减半（本地推入与副机推回均从 16px 降低为 8px，轻推即过）；
  从机深入武装距离从 30px 降低为 6px 且严格按向副机内部深入方向计算，彻底解除靠近屏幕边缘操作时无法退出的卡死现象。
- **修复**：控制台「运行日志」被清空或已读后，右上角的未读消息气泡现在会正确隐藏
  （此前「清空」后气泡仍残留显示旧未读数）；未读数统一由 `setUnread` 维护，
  控制台打开期间新日志不再误计入未读。
- **新增**：传输动态记录本地持久化（localStorage，`cw-feed`，保留最近 200 条），
  刷新/重开面板不再丢失历史；加载时与服务端本次运行记录按键去重合并。
- **新增**：文本同步也计入传输动态——本机发送与对端接收的文本均产生记录
  （`ui.FileRecord` 新增 `Text` 标记，Name 为内容首行预览，最多 40 字符），
  面板与 TUI「文件记录」页均可看到。
- **新增**：重复 copy 去重——与上一条同方向/同对端/同名同大小的记录合并为一条，
  以 ×N 显示重复次数并刷新时间与状态。
- **改进**：传输动态重构为通用**对话消息界面**（参考企业微信）——左侧收
  （对端头像 + 名字 + 时间在气泡上方）、右侧发（本机身份镜像排列，气泡着色），
  并**针对常见类型做消息预览**：文本消息直接显示内容（超长折叠、点击展开，
  收到的文本可一键复制回剪贴板）；文件消息为卡片式——按扩展名显示彩色类型
  徽标（DOC/XLS/PPT/PDF/ZIP/图片/音视频/可执行/代码等）+ 文件名 + 大小；
  接收目录内的图片显示**缩略图预览**（新增只读端点 `GET /api/file`，仅限
  接收目录内常见图片扩展名，防止任意文件读取；文件被移动后自动回退卡片）。
  消息操作改为**双击直达**：收到的文件双击打开（不再用悬停按钮），收到的
  文本双击复制回剪贴板；平时不显示「已接收/已发送」状态，仅失败时保留红色
  状态提示。
  工作台底部新增**对话输入框**：文本输入回车即发（中文输入法选词 Enter 不
  误发）、附件按钮选择文件发送，顶栏「发送文本」与 Ctrl+K「发送文本…」改为
  聚焦输入框（移除原弹窗）；无在线节点时占位文案给出提示。文本记录新增
  限长完整内容（`textDetail`，4096 字符），前端持久化时再截断至 2000 字符
  保护 localStorage。
- **新增**：**截图同步**——Win+Shift+S / PrintScreen 等直接写入剪贴板的截图
  现在会被监听并自动同步（此前只认文件与文本）。优先读取截图工具放入的
  "PNG" 注册格式，否则将 CF_DIB 位图就地转换为 PNG（支持 24/32bpp 的
  BI_RGB / BI_BITFIELDS）；截图落盘到接收目录的「截图」子目录后按普通文件
  通道发送与重试，按内容哈希去重、受容量阈值约束；带文本的位图（如 Excel
  复制附带）仍按文本处理，不会误发截图。
- **改进**：交互审计修复（详见 `docs/webui-ux-audit.md`）——
  **接收进度**：传输协议新增接收方向进度回调，面板对收到的文件也显示实时
  进度气泡（`Progress` 新增 `In` 方向标记，`transport.Handlers` 新增
  `OnProgress`，每 256KB 上报、失败报 -1 收起）；**消息不再丢**：SSE 断线
  重连后自动补拉 `/api/records`（"清空记录"时刻之后的记录才补入）；
  **增量渲染**：新消息只追加/原地更新末行，不再整表重建，不打断悬停与选区；
  **日期分隔**：记录携带日期字段，时间线按 今天/昨天/日期 分组；向上翻历史
  时浮现「↓ 回到最新（N 条新消息）」按钮；**发送可取消**：上传进度条增加
  ✕ 取消按钮（XHR abort），上传完成但无发送进度时明确提示检查配对；
  **文案对齐**：移除名不符实的顶栏「发送文本」按钮（命令面板保留），
  无节点时输入框占位符如实提示且回车立即明确报错；**输入框多行**：改为
  自动增高 textarea，Shift+Enter 换行、回车发送；**图片灯箱**：点击缩略图
  面板内放大（双击仍打开文件）；**统一确认弹窗**替代原生 confirm
  （清空记录、解除配对）；错误 toast 驻留 8s 且全部可点击关闭；「已被拒」
  tag 悬浮解释原因；配对请求弹窗不再响应 Esc（必须明确裁决），设备坞显示
  「待确认」标记；日志错误行着色、重新勾选自动滚动立即回底；节点名留空
  保存给出明确错误；进度气泡代际修复（同名传输连续进行不再被旧定时器误删）；
  Ctrl+1/2 快捷切换工作台/屏幕布局；图标按钮补齐 aria-label；窄屏防溢出。

## v0.3.0 (2026-09-11)

- **配置全部即时生效，取消「保存配置」按钮**：设置面板每个控件改动后立即
  保存并生效，无需再点保存、更无需重启。**启用/停用鼠标键盘跨屏改为热
  启停**——开启即启动 KVM 监听、停用即关闭监听并结束活动会话（输入钩子
  进程级常驻，经门禁转发，未启用时空操作）；**节点名热生效**——发现广播
  每次发送时取当前名（`discovery.RunDynamic`），改名后下一次广播（约几秒）
  即同步到对端；阈值/接收目录/自动回贴/文本同步/跨屏手感等原本已即时生效。
  `kvm.Service` 新增 `Stop`（热停用并结束主控/被控会话），`App` 新增
  `SetKVMEnabled`。
- **修复 KVM 布局"取消/换人不同步"**：此前面板取消某侧邻居（清空）或把该侧
  换成其他节点时，只在本机生效，旧邻居收不到任何通知，导致对端残留过期的
  邻居配置、跨屏切换结果不对。现协议 `kvm` 类型新增 `clear` 语义（把我从你
  的某侧移除），保存布局时携带改动前的邻居：取消或换人都会向旧邻居推送取消
  同步；接收端带名字守卫，迟到的取消消息不会覆盖本机较新的布局
  （`KVMSync.Clear`、`App.SyncKVMFrom`）。
  **注意**：v0.2 及更早对端不认识 `clear` 字段，会把取消消息误解为"设置"
  ——混版运行期间请勿使用取消/换人，且 `kvm` 协议有变更，
  **所有节点必须同步升级到 v0.3.0**。
- **面板信息架构全面重构（单屏工作台范式）**：抛弃「侧栏多页签后台」布局，参考
  Raycast / LocalSend 等现代工具软件重排为——顶部线框工具栏（空间切换、
  全局状态、发送操作、主题/日志/设置）、左侧**持久设备坞**（两个空间均保持
  可见：本机 + 在线节点卡片，配对 / 设为左右邻直接在卡片上完成）、中央聊天式
  传输时间线（收发气泡化，
  左收右发，进行中传输为带进度条的实时气泡）、底部状态栏（本机信息 / 跨屏
  状态 / 版本）；新增 **Ctrl+K 命令面板**：发送、暂停同步、跨屏开关、切换
  主题、空间跳转、解除配对等全部操作可键盘直达；设置改为居中大面板
  （分类导航 + 行式配置项 + 重启生效徽标）；传输时间线改为**时间升序**
  （旧→新，聊天式阅读顺序），进行中传输的实时进度气泡固定在时间线底部并
  自动跟随滚动（向上翻阅历史时不打扰）；Toast 提示移至底部居中；工作台与
  屏幕布局两个空间的切换加入淡入过渡（遵循系统"减弱动态效果"设置）；视觉
  整体改为**极客线框 / 终端风**：等宽字体、全直角、1px 线框与虚线分隔、
  蓝图网格背景、扁平终端配色（青色 accent）、斜纹进度条与括号式状态徽标，
  去除玻璃拟态 / 渐变 / 光晕，深浅双主题（深色终端 / 浅色蓝图纸）均适配；
  **仅托盘菜单图标重绘为浅色线框风格**（`tools/icon/gen_menu_icons.ps1`）：
  浅纸底 + 深灰细描边 + 深墨线稿（打开面板/暂停/恢复/接收目录/退出/暂停态
  徽章 6 枚，恢复绿、退出红、琥珀暂停角标点缀）；主体应用图标 icon.ico、
  favicon 与 exe 内嵌资源保持原折纸光翼渐变设计不变

- **接收目录重新规划：每次接收独立子目录，文件不再重命名**。接收目录下按
  `<时间戳>-<发送方>` 建立本次传输的专属子目录，收到的文件一律保留原始文件名
  （此前重名文件被追加 " (n)" 改名）；多文件/文件夹传输解包后内容也集中在同一子目录
- **多文件/文件夹自动解包**：发送端因多文件/目录自动打包的合成 zip 传输时携带
  `bundle` 标记，接收端自动解包并回贴原内容（文件夹仍是文件夹、多文件仍是多文件，
  Ctrl+V 粘贴与发送端复制的一致）；用户亲手发送的 zip 文件不带标记，原样保留不误解包。
  旧版本对端会把合成 zip 当普通文件保存，行为向后兼容
- **修复 KVM 边缘切换时灵时不灵**：主控端贴边推动的累积改为反向抵消（下限 0）
  而非一笔清零——高回报率鼠标贴边斜推时 dx 常在 ±1 间抖动，此前任何一次反向
  事件都清空累积，导致永远凑不满触发阈值；被控端切回判定同样改为双向抵消，
  贴边微抖不再误触；切换失败后的重试锁定与冷却统一为 1.5s（此前锁死 3s，
  且冷却字段从未生效）；修复被控端 enter 时入口字段锁外写入与切回检测的数据竞争
- **双击 exe 直接进入 gui 模式**：无参数启动等价 `gui`（托盘常驻 + 浏览器自动
  打开控制面板），双击启动时自动隐藏随进程创建的控制台窗口（终端里执行不隐藏，
  日志照常可见）；gui 启动失败（如已有实例在运行）时弹出消息框提示，不再静默退出
- **应用图标重新设计**：渐变圆角底 + 白色剪贴板 + 双向同步箭头，覆盖
  16~256 全尺寸；exe 打包嵌入同一图标（`rsrc_windows_amd64.syso`，
  含版本信息资源），系统托盘与面板 favicon 同步使用（`tools/icon/gen_icon.ps1`）

- **破坏性变更：废弃共享 token 认证**。config.json 不再有 `token` 字段，
  鉴权只认配对令牌——未配对的节点无法传输、无法 KVM 跨屏。
  （此前"各节点 token 改成一致"的互通用法随之失效，请升级后在面板重新配对）
- **KVM 布局跨机自动同步**：面板「屏幕布局」配置左右邻后，布局经配对通道推送
  到对端（自动互为镜像），本机与对端均即时热生效，无需重启、无需两端重复配置；
  新增传输类型 `kvm` 承载该同步
- 新增**配对认证**：gui 面板「在线节点」点击「配对」→ 对端面板确认
  「接受配对」即完成；双方交换逐对独立的配对令牌，存于 `~/.copywhere/peers.json`
  （新增，支持解除配对/吊销）（`internal/trust`）
- 传输协议新增 `pair` 类型（人工确认的凭据交换，应答沿原连接返回不可伪造）；
  协议头新增 `sender_id` 字段用于配对令牌校验
- 面板新增「屏幕布局」视图：仿 Windows 多显示器设置，设备渲染为显示器卡片，
  拖动到本机左右空位即完成 KVM 左邻/右邻配置（支持点选快捷按钮、交换位置、
  单侧清除、离线邻居灰色展示）；KVM 开关可在面板编辑（开关重启生效）
- 面板「在线节点」显示配对状态（已配对/未配对/已被拒）与配对按钮；
  收到配对请求弹出确认框；设置页新增「已配对设备」管理
- 新增 GUI 模式（`copywhere gui`）：系统托盘 + 浏览器控制面板，
  与 TUI（`run`）共存；面板以传输动态为主页（收发时间线 + 实时进度），
  支持全局拖拽投送、卡片式节点列表、终端风格日志抽屉、侧滑设置面板、
  深浅双主题（`internal/webui`）
- 托盘菜单：打开面板、暂停/恢复自动同步、打开接收目录、退出
- 手动发送支持面板拖入/选择文件上传（不受阈值限制）与文本发送，
  大文件传输实时进度条
- 面板设置页：节点名、自动阈值、接收目录、自动回贴、文本同步
  在线编辑（节点名重启生效，其余立即生效）
- 接收记录可从面板一键打开文件所在目录；新增 `web_port` 配置项
  （默认 47890，负数=随机端口），面板仅监听 127.0.0.1 + 随机访问令牌
- 剪贴板监控支持暂停开关（暂停期间内容不累积，恢复后不补发），
  阈值与文本同步开关修改后即时生效
- `gui` 以命名互斥锁防止与 `run` 双实例运行；面板地址落盘
  `~/.copywhere/panel_url.txt` 备查

## v0.2.0 (2026-09-09)

- 新增鼠标键盘跨屏（KVM，实验性）：边缘推动切换、输入转发与注入、
  看门狗自动释放、Ctrl+Alt+Shift+X 紧急退出（`internal/input`、`internal/kvm`）
- KVM 多显示器支持：暴露边缘检测（双屏内侧边界不误触发）、被控入口恒在
  主显示器共享边缘、相对位移注入（副机本地多屏自然跨越）、可配置入口屏
  （`kvm_entry_monitor`）
- KVM 入口点按主控光标比例映射到被控主屏同比例高度；绝对定位改用
  `SetCursorPos`（物理虚拟桌面像素），修复被控双屏时入口总跑到主屏角落
- DPI 感知升级 Per-Monitor v2（Win10 1703+，失败退回 System Aware），
  多屏不同缩放率下枚举与注入坐标系一致
- 修复 KVM 钩子线程死锁导致全系统鼠标卡顿（显示器缓存改独立锁 + 后台刷新）
- 修复 KVM accept 瞬时错误导致监听静默退出（表现为握手超时）
- 修复 KVM 空闲断连：被控端回应 ping（pong），主控端读超时不再误判
- 回退 Raw Input 设备句柄过滤（部分物理鼠标无句柄导致跨屏失效）
- 地址优选：192.168 局域网恒优先于虚拟网段（ZeroTier 10.*）

## v0.1.0 (2026-09-08)

首个版本。

- 节点自动发现：UDP 广播（各网段定向广播 + 回环），稳定机器指纹 + 会话标识
- 多网卡地址合并：同一节点全部地址保留，同网段直连优选，发送逐地址尝试
- 剪贴板同步：文件（CF_HDROP）与文本自动推送，接收端自动回贴剪贴板
- 三页终端界面：日志 / 在线节点 / 文件记录（Tab/←→ 切换，↑↓/PgUp/PgDn 滚动）
- TCP 传输：sha256 校验、临时文件原子改名、同名同内容去重
- 慢链路友好超时预算（256KB/s 估算），失败原因记录在对端日志
- token 鉴权；不一致提示一次即暂停发送，对端重启后自动恢复
- 多文件/目录自动打包 zip；`send` 命令手动发送不受阈值限制
- 防回环：剪贴板序号 + 接收冷却期双重防护
- 配置位于 `~/.copywhere/config.json`，接收目录 `~/.copywhere/files`
